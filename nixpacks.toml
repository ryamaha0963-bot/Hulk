[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = [
    "python3 -m venv venv",
    ". venv/bin/activate",
    "pip install --no-cache-dir -r requirements.txt"
]

[start]
cmd = ". venv/bin/activate && bash start.sh"
